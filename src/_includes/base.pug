include formatters/format_date.pug
include formatters/format_tag.pug

- var currentYear = new Date().getFullYear()

doctype html

html(lang="en")
    head
        title= metas.site + " | Blog"
        include meta.pug

body
    #header
        h1
            a(href="/") #{metas.name}<br>#{metas.surname}

    #content
        include posts.pug

    include social.pug

    include footer.pug
